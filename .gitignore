node_modules/
dist/
.next/
coverage/
*.log
.DS_Store

# local environment files (keep templates only)
.env
.env.*
!.env.example
!.env.*.example
**/.env
**/.env.*
!**/.env.example
!**/.env.*.example

# common local secrets/certs
*.pem
*.key
*.p12
*.pfx
*.jks

# external local repo root (not committed in nimi monorepo)
nimi-mods/

# tauri/rust
**/target/
/runtime/nimi
/runtime/nimi.exe

# local model files (large binaries, partial downloads)
/runtime/models/

# local caches
.vscode/
.idea/
.cache/

_external/
.claude/settings.local.json

# legacy execution-doc roots are archive/local-only
dev/

# repo-tracked execution support inputs live under nimi-coding/config and nimi-coding/fixtures
nimi-coding/.local/report/release/

# local AI coding artifacts (never committed)
.local/

# local scratch outputs
.tmp/
