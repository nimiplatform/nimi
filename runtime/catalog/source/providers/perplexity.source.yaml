schema_version: 3
provider: perplexity
catalog_version: 2026-04-18-perplexity-v3
generated_target: runtime/catalog/providers/perplexity.yaml
runtime:
  runtime_plane: remote
  managed_connector_supported: true
  inline_supported: true
  default_endpoint: null
  requires_explicit_endpoint: true
  inventory_mode: dynamic_endpoint
  dynamic_inventory:
    discovery_transport: connector_list_models
    cache_ttl_sec: 300
    selection_mode: pass_through
    failure_policy: use_cache_then_fail_closed
    allowed_capabilities:
      - text.generate
      - text.generate.vision
defaults:
  provider: perplexity
  model_type: chat
  capabilities: []
  pricing:
    unit: token
    input: unknown
    output: unknown
    currency: USD
    as_of: 2026-04-18
    notes: Perplexity Agent API publishes a live OpenAI-compatible `GET /v1/models` endpoint across Perplexity and third-party models, so runtime inventory should be discovered dynamically instead of curated as static source rows.

sources:
  - source_id: perplexity_models_doc
    url: https://docs.perplexity.ai/docs/agent-api/openai-compatibility
    retrieved_at: 2026-04-18
    note: Perplexity Agent API OpenAI-compatibility guide documenting `GET /v1/models` for live model discovery.
