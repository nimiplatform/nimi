schema_version: 3
provider: together
catalog_version: 2026-04-18-together-v2
generated_target: runtime/catalog/providers/together.yaml
runtime:
  runtime_plane: remote
  managed_connector_supported: true
  inline_supported: true
  default_endpoint: null
  requires_explicit_endpoint: true
  inventory_mode: dynamic_endpoint
  dynamic_inventory:
    discovery_transport: connector_list_models
    cache_ttl_sec: 300
    selection_mode: pass_through
    failure_policy: use_cache_then_fail_closed
    allowed_capabilities:
      - text.generate
      - text.generate.vision
      - text.embed
      - image.generate
      - video.generate
      - audio.synthesize
      - audio.transcribe
defaults:
  provider: together
  model_type: chat
  capabilities: []
  pricing:
    unit: token
    input: unknown
    output: unknown
    currency: USD
    as_of: 2026-04-18
    notes: Together Serverless exposes a broad, fast-changing multi-provider model marketplace across text, image, video, audio, embedding, and moderation surfaces, so runtime inventory should be discovered dynamically instead of curated as static source rows.

sources:
  - source_id: together_models_doc
    url: https://docs.together.ai/docs/serverless-models
    retrieved_at: 2026-04-18
    note: Together AI serverless catalog spanning chat, image, vision, video, audio, embedding, rerank, and moderation models.
