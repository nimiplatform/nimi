# nimi-shell-protected-local-node

Host-only Node-API projection of `nimi-shell-protected-local` for Electron main
processes. The addon exposes only installed-session open and typed Runtime
artifact reads. It does not expose a generic RPC proxy, session proof,
credential, endpoint, app identity, release identity, or renderer API.
