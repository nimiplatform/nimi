import { NIMI_STANDARD_SHELL_COMMANDS } from './commands.js';

export const NIMI_LOCAL_ASSETS_SHELL_COMMANDS = Object.freeze({
  resolveUrl: NIMI_STANDARD_SHELL_COMMANDS['local-assets.resolveUrl'],
});
