import { NIMI_STANDARD_SHELL_COMMANDS } from './commands.js';

export const NIMI_DIAGNOSTICS_SHELL_COMMANDS = Object.freeze({
  rendererEntryProbe: NIMI_STANDARD_SHELL_COMMANDS['diagnostics.rendererEntryProbe'],
});
