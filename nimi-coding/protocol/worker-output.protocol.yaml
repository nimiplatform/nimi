id: worker-output.v1
purpose: Normalize worker completion output for audit and acceptance.
requires:
  - findings
  - implementation summary
  - files changed
  - checks run
  - remaining gaps / risks
  - machine-readable runner signal when the output participates in provider-backed continuous execution
forbids:
  - completion without checks
  - narrative-only output
  - partial work labeled as complete
