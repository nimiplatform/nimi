name: deploy-install-gateway

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-install-gateway
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      NIMI_GITHUB_RELEASES_TOKEN: ${{ secrets.NIMI_GITHUB_RELEASES_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup pnpm
        uses: pnpm/action-setup@903f9c1a6ebcba6cf41d87230be49611ac97822e # v6.0.3
        with:
          version: 10.32.1

      - name: Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 24
          package-manager-cache: false

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Test install gateway
        run: pnpm --filter @nimiplatform/install-gateway test

      - name: Build install gateway assets
        run: pnpm --filter @nimiplatform/install-gateway build

      - name: Sync optional GitHub release token secret
        if: env.NIMI_GITHUB_RELEASES_TOKEN != ''
        run: |
          set -euo pipefail
          cd apps/install-gateway
          printf '%s' "${NIMI_GITHUB_RELEASES_TOKEN}" | pnpm dlx wrangler secret put NIMI_GITHUB_RELEASES_TOKEN --config wrangler.jsonc

      - name: Deploy install gateway worker
        run: |
          set -euo pipefail
          cd apps/install-gateway
          pnpm dlx wrangler deploy --config wrangler.jsonc
