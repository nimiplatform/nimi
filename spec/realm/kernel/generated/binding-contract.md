<!-- DO NOT EDIT: generated by scripts/generate-realm-spec-kernel-docs.mjs -->
<!-- Source: spec/realm/kernel/tables/binding-contract.yaml -->

# Binding Contract (Generated)

Contract: `BINDING-CONTRACT-001`
Domain: `binding`
Version: `2026-03-26`

Rules: 5

| Rule ID | Level | Title | Statement |
| --- | --- | --- | --- |
| R-BIND-001 | must | binding-is-only-durable-object-to-host-relation | Realm Binding is the only formal durable relation used to attach Realm objects to Realm hosts for presentation, use, or import semantics; Attachment remains a display envelope only. |
| R-BIND-002 | must | fixed-active-binding-object-and-host-types | Active binding object types are RESOURCE, ASSET, and BUNDLE; active binding host types are WORLD, AGENT, SCENE, WORLD_EVENT, and WORLDVIEW; WORKSPACE is outside the active backend binding model until a host contract exists. |
| R-BIND-003 | must | allow-matrix-fail-close | Binding legality is fail-close and matrix-governed; only declared bindingKind, objectType, hostType, and bindingPoint combinations are valid, and all undeclared combinations must be rejected. |
| R-BIND-004 | must | typed-conditions-and-import-version-pin | PRESENTATION bindings may carry flat string-map conditions and explicit binding points; USE and IMPORT bindings must not carry conditions; IMPORT bindings must pin the imported bundle version and do not imply member USE bindings. |
| R-BIND-005 | must | explicit-idempotent-world-scoped-binding-writes | Binding writes are explicit, idempotent, and auditable; every binding resolves to a world scope, records the creating actor, and uses the same logical uniqueness key for validation, persistence, and upsert behavior. |

Entities: 1

| Entity | Prisma Model | Required Fields | JSON Fields |
| --- | --- | --- | --- |
| Binding | Binding | id, objectType, objectId, hostType, hostId, bindingKind, priority, conditionHash, scopeWorldId, createdBy | conditions |

Required operations: 4
- GET /api/world/by-id/{id}/bindings
- GET /api/worlds/{worldId}/bindings
- POST /api/worlds/{worldId}/bindings
- DELETE /api/worlds/{worldId}/bindings/{bindingId}

Secondary operations: 3
- GET /api/resources/{resourceId}
- GET /api/assets/{assetId}
- GET /api/bundles/{bundleId}
