# Agent Public Boundary

> Domain: Realm / Agent
> Status: Active
> Date: 2026-03-01

## 0. Normative Imports

| Kernel Location | Rule IDs |
|---|---|
| `kernel/boundary-vocabulary-contract.md` | R-BOUND-002, R-BOUND-010 |

## 1. Scope

Agent 在 public 范围提供跨域可依赖边界（R-BOUND-002）。

## 2. 有意薄桩声明

本文件为有意薄桩（intentional thin stub）。Agent 域的实现规则（行为策略 DSL、memory 评分算法、persona/brain/worldview 配置模板）定义在闭源 nimi-realm 仓库。本文件仅承载公共边界词汇引用，不包含域特有增量规则。

公共词汇定义见 `kernel/tables/public-vocabulary.yaml`（agent 域）。

## 3. 非目标

本文件不定义：agent 行为策略 DSL、私有 memory 评分算法、内部 persona/brain/worldview 配置模板。
