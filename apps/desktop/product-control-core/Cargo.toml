[package]
name = "nimi-desktop-product-control-core"
version = "0.1.0"
edition = "2021"
description = "Retired Desktop product-control native crate placeholder."
license = "MIT"

[dependencies]
chrono = { version = "0.4", default-features = true, features = ["clock"] }
dirs = "6"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
