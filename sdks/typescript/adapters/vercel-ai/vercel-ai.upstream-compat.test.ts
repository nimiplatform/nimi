// Upstream compatibility suite: mirrors Vercel AI SDK repository test
// dimensions through the public `ai` and `ai/test` entrypoints. These cases are
// adapter-provider contract tests, not copies of Vercel's internal unit tests.
import './vercel-ai.upstream-text-compat.test';
import './vercel-ai.upstream-tool-compat.test';
import './vercel-ai.upstream-object-compat.test';
