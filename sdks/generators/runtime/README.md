# Runtime Generator

Runtime core manifests are generated from `proto/runtime/v1/*.proto`.
`runtime-method-groups.yaml` is intentionally not an input because it governs
only the current `sdk/` TypeScript app-facing facade projection.

