# Shared Generator Outputs

This directory holds generated shared manifests for Runtime core, Realm core,
SDK error codes, and export manifests. Files under `generated/` are produced by
`sdks/generators/generate.mjs`.
