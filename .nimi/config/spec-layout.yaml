version: 1
contract_ref: .nimi/contracts/spec-layout.schema.yaml
spec_layout:
  canonical_root: .nimi/spec
  host_instruction_paths: []
  tracked_derived_projections: []
  table_family_extensions:
    - table_family: simulator_performance_policy
      authority_class: product_authority_table
      required_fields:
        - table_family
        - owner
        - catalog_id
        - measurement_contract
        - invariants
        - scenarios
        - calibration_admission
      forbidden_fields:
        - done
        - covered
        - coverage_status
        - audit_date
        - evidence_report
        - current
        - proposed
        - backlog_status
        - migration_status
        - mapping_status
        - run_id
        - ledger_ref
