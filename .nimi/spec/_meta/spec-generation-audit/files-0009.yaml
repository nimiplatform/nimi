version: 1
contract_ref: .nimi/contracts/spec-generation-audit.schema.yaml
spec_generation_audit_file_entries:
  parent_ref: .nimi/spec/_meta/spec-generation-audit.yaml
  files:
    - canonical_path: .nimi/spec/runtime/kernel/tables/agent-participation-axis-model.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/agent-participation-axis-model.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at 91a77363c spec(runtime): admit agent
          participation authority."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/agent-participation-capability-scopes.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/agent-participation-capability-scopes.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at 91a77363c spec(runtime): admit agent
          participation authority."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/agent-participation-concurrency-policy.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/agent-participation-concurrency-policy.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at 91a77363c spec(runtime): admit agent
          participation authority."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/agent-participation-context-blocks.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/agent-participation-context-blocks.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at 91a77363c spec(runtime): admit agent
          participation authority."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/agent-participation-domain-future-seams.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/agent-participation-domain-future-seams.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at 4dc43fd54 spec(runtime): extend
          agent participation boundaries."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/agent-participation-external-entry-boundaries.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/agent-participation-external-entry-boundaries.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at 4dc43fd54 spec(runtime): extend
          agent participation boundaries."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/agent-participation-memory-policy.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/agent-participation-memory-policy.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at 91a77363c spec(runtime): admit agent
          participation authority."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/agent-participation-memory-read-scopes.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/agent-participation-memory-read-scopes.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at 91a77363c spec(runtime): admit agent
          participation authority."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/agent-participation-output-destinations.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/agent-participation-output-destinations.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at 91a77363c spec(runtime): admit agent
          participation authority."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/agent-participation-profiles.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/agent-participation-profiles.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at 91a77363c spec(runtime): admit agent
          participation authority."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/agent-participation-promotion-boundaries.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/agent-participation-promotion-boundaries.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at 4dc43fd54 spec(runtime): extend
          agent participation boundaries."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/avatar-debug-probe-events.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/avatar-debug-probe-events.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at 97e4d5f16 spec: admit avatar debug
          workbench authority."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/avatar-debug-replay-keys.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/avatar-debug-replay-keys.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at 97e4d5f16 spec: admit avatar debug
          workbench authority."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/delegation-protocol-adapters.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/delegation-protocol-adapters.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at ce2a6f653 spec: admit delegated
          capability gateway."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/delegation-provider-profiles.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/delegation-provider-profiles.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at ce2a6f653 spec: admit delegated
          capability gateway."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/delegation-reason-codes.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/delegation-reason-codes.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at ce2a6f653 spec: admit delegated
          capability gateway."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/delegation-request-fields.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/delegation-request-fields.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at ce2a6f653 spec: admit delegated
          capability gateway."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/delegation-result-fields.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/delegation-result-fields.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at ce2a6f653 spec: admit delegated
          capability gateway."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/host-accelerator-profiles.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/host-accelerator-profiles.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at f0466ccf5 docs: align local runtime
          accelerator contracts."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/host-capability-profiles.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/host-capability-profiles.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at 1f250898c spec: define local
          environment runtime authority."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/local-compute-packs.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/local-compute-packs.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at 1f250898c spec: define local
          environment runtime authority."
        - "Latest observed spec change before ledger closure: 188a0c167 spec:
          define local environment activation gates."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/local-environment-consumer-requirements.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/local-environment-consumer-requirements.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at 1f250898c spec: define local
          environment runtime authority."
        - "Latest observed spec change before ledger closure: 188a0c167 spec:
          define local environment activation gates."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/local-environment-dependencies.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/local-environment-dependencies.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at 1f250898c spec: define local
          environment runtime authority."
        - "Latest observed spec change before ledger closure: 188a0c167 spec:
          define local environment activation gates."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/local-environment-job-states.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/local-environment-job-states.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at 1f250898c spec: define local
          environment runtime authority."
        - "Latest observed spec change before ledger closure: 188a0c167 spec:
          define local environment activation gates."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/local-environment-materializers.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/local-environment-materializers.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at 19b54e8e0 spec: define local
          environment materializers."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/local-environment-source-manifests.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/local-environment-source-manifests.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at 19b54e8e0 spec: define local
          environment materializers."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/local-environment-verification-evidence.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/local-environment-verification-evidence.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at 19b54e8e0 spec: define local
          environment materializers."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/selected-source-record-schema.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/selected-source-record-schema.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at 1f250898c spec: define local
          environment runtime authority."
        - "Latest observed spec change before ledger closure: 188a0c167 spec:
          define local environment activation gates."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/runtime/kernel/tables/shared-accelerator-dependencies.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/runtime/kernel/tables/shared-accelerator-dependencies.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at f0466ccf5 docs: align local runtime
          accelerator contracts."
        - "Latest observed spec change before ledger closure: 1f250898c spec:
          define local environment runtime authority."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/sdk/kernel/generated/runtime-agent-participation-methods.md
      file_class: kernel_generated
      source_refs:
        - .nimi/spec/sdk/kernel/tables/runtime-agent-participation-methods.yaml
        - .nimi/spec/sdk/kernel/generated/runtime-agent-participation-methods.md
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at d926d725e spec(sdk): project runtime
          agent participation client."
        - Generated markdown source table recovered as
          .nimi/spec/sdk/kernel/tables/runtime-agent-participation-methods.yaml.
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/sdk/kernel/local-environment-projection-contract.md
      file_class: kernel_markdown
      source_refs:
        - .nimi/spec/sdk/kernel/local-environment-projection-contract.md
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at 1f250898c spec: define local
          environment runtime authority."
        - "Latest observed spec change before ledger closure: 19b54e8e0 spec:
          define local environment materializers."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/sdk/kernel/runtime-agent-participation-client-contract.md
      file_class: kernel_markdown
      source_refs:
        - .nimi/spec/sdk/kernel/runtime-agent-participation-client-contract.md
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at d926d725e spec(sdk): project runtime
          agent participation client."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/sdk/kernel/runtime-avatar-control-client-contract.md
      file_class: kernel_markdown
      source_refs:
        - .nimi/spec/sdk/kernel/runtime-avatar-control-client-contract.md
      source_basis: grounded
      coverage_status: complete
      unresolved_items: []
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at 97e4d5f16 spec: admit avatar debug
          workbench authority."
    - canonical_path: .nimi/spec/sdk/kernel/runtime-delegation-client-contract.md
      file_class: kernel_markdown
      source_refs:
        - .nimi/spec/sdk/kernel/runtime-delegation-client-contract.md
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at ce2a6f653 spec: admit delegated
          capability gateway."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/sdk/kernel/tables/runtime-agent-participation-methods.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/sdk/kernel/tables/runtime-agent-participation-methods.yaml
      source_basis: mixed_grounded_and_inferred
      coverage_status: partial
      unresolved_items:
        - Original generation topic was not embedded in the canonical file;
          provenance was recovered from path/domain lineage, topic corpus, and
          Git history during audit-ledger closure.
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at d926d725e spec(sdk): project runtime
          agent participation client."
        - Lineage was recovered from stable canonical paths and Git history
          rather than from local topic source refs.
    - canonical_path: .nimi/spec/sdk/kernel/tables/runtime-avatar-control-methods.yaml
      file_class: kernel_tables
      source_refs:
        - .nimi/spec/sdk/kernel/tables/runtime-avatar-control-methods.yaml
      source_basis: grounded
      coverage_status: complete
      unresolved_items: []
      notes:
        - Added during the 2026-05-07 audit-ledger closure pass; canonical spec
          content was not changed.
        - "Git provenance first observed at 97e4d5f16 spec: admit avatar debug
          workbench authority."
