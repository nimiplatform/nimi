table_family: product_catalog
owner: desktop
catalog_id: desktop_realm_agent_creation_modes
authority_refs:
  - D-EXPL-008
entries:
  - id: manual-quick-create
    name: Manual quick create
    semantics: User fills the minimum RealmAgent fields directly.
  - id: character-card-import
    name: Character Card import
    semantics: User imports a Character Card-style file; Nimi parses locally, maps fields, and shows review before any Realm truth write.
  - id: ai-assisted-generation
    name: AI-assisted generation
    semantics: User describes the concept; Nimi generates a candidate under an explicit AIScopeRef and shows review before any Realm truth write.
