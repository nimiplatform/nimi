# Avatar Kernel — Migrated Domain Index

本域契约散文与表已全量迁入 canonical authority(2026-07-25)。

## Canonical 容器(规范权威)

| 容器 | 覆盖面 |
|---|---|
| `.nimi/spec/avatar/embodiment-surface.authority.yaml` | Avatar 产品面:backend 分支(live2d/vrm/nimi2d)/embodiment 投影输出/avatar.* 事件族/NAS 投影 API/shell 生命周期与安全/生成动作 provider/Live2D 适配与兼容层级/VRM 生命周期/窗口可见域与 fail-closed 语义 |
| `.nimi/spec/runtime/agent-participation.authority.yaml` | Runtime 产权:companion 参与/触发/拒绝、外部入口消费边界、wake 与本地音频生命周期、调试会话接收、NAS 执行约束、runtime.agent.* 事件溯源、投影输入本体 |

机器行数据见 `config/avatar-*.yaml`，mock 场景与样例目录为 app
本地普通 fixture；它们都不是产品权威。

不得基于本索引推断权威;权威只在 canonical 容器。
