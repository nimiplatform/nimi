# Optional Cognition Bridge — Domain Index

This area contains one bounded bridge for an optional independent
implementation. It is not a standalone product authority domain and is
not a prerequisite for Runtime readiness.

| Container | Scope |
| --- | --- |
| `.nimi/spec/cognition/runtime-bridge.authority.yaml` | A one-way, no-downgrade integration boundary that does not transfer LocalAgent, Conversation, Memory, or Knowledge ownership away from Runtime |

Do not infer product authority from this index. Canonical truth exists
only in the referenced authority container.
