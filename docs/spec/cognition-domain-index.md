# Cognition Kernel — Migrated Domain Index

本域契约散文与表已全量迁入 canonical authority(2026-07-25)。

## Canonical 容器(规范权威)

| 容器 | 覆盖面 |
|---|---|
| `.nimi/spec/cognition/standalone-services.authority.yaml` | 独立认知域:对象/family/scope 模型、memory/knowledge/skill/prompt/refgraph 服务契约、app 记忆访问安全边界、公开面与失败模型、完成语义闭合 |
| `.nimi/spec/cognition/runtime-bridge.authority.yaml` | 认知侧桥接:独立性与桥方向、no-downgrade 一致性矩阵、typed knowledge scope、authorizer 缝(Runtime 侧席位由 runtime memory-world 既有单元覆盖,零新增) |

原文散文见 `docs/authority/cognition-standalone-services-rationale.md`(非规范)。C-COG-*/C-APMEM-* 旧规则 ID 已携带于 canonical 单元标题,代码注释锚保持可查。

不得基于本索引推断权威;权威只在 canonical 容器。
