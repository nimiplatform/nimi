# Nimi2D Kernel — Migrated Domain Index

本域契约散文与表已全量迁入 canonical authority(2026-07-25)。

## Canonical 容器(规范权威)

| 容器 | 覆盖面 |
|---|---|
| `.nimi/spec/nimi2d/asset-package.authority.yaml` | 资产包真相:layer 输入/包 manifest 线契约、typed rejects、base-body 拓扑、slot/wardrobe、能力层级矩阵、no-outfit/no-render 与内容准入安全边界 |
| `.nimi/spec/nimi2d/generation-supply.authority.yaml` | 生成供给:provider 中性请求/工件线契约、pre-admission 地位、fail-closed 响应、go/no_go 生成可行性语义与渲染性/安全硬门 |

原文散文见 `docs/authority/nimi2d-asset-package-rationale.md`(非规范)。数值 bench 阈值属调参配置(非权威);corpus/result 证据 schema 已按类 1/2/3 拒绝入 Git 史。

不得基于本索引推断权威;权威只在 canonical 容器。
