# Roadmap

Nimi no longer keeps candidate roadmap authority under `.nimi/spec/future`.
Active product authority lives under `.nimi/spec/**`; roadmap and backlog material
are handled through topic lifecycle records until a capability is admitted into an
active product domain.

This page is intentionally non-authoritative. Use the active domain spec for
implemented product behavior and topic records for candidate planning context.
